import { useState } from 'react';

const Second = (props) => {
    const {setStudents, students} = props;

    return ( //JSX
        <div>
            Second Hurra!
        </div>
    )
}

export default Second;